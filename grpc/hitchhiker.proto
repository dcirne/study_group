syntax = "proto3";

package hitchhiker;

service Computer {
    rpc Inquire(Question) returns(Answer) {}
    rpc Inquires(stream Question) returns(stream Answer) {}
}

message Question {
    string text = 1;
}

message Answer {
    string text = 1;
}

